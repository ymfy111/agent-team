# 多智能体协作产品化建议-v0.6.33.md 引用链补丁（TF-DOC-MERGE-01）

> 目标文件：`docs/recommendations/多智能体协作产品化建议-v0.6.33.md`  
> 补丁性质：人工合并提示。

## 建议插入位置 1：文档开头「定位说明」后

```md
> TaskFlow / TaskTicket 层级模型、字段映射和 P0 文档化落地约定详见 `docs/specs/SDD-TASKFLOW-TASKTICKET-MODEL-v0.6.33.md`。
```

## 建议插入位置 2：3.0 TaskTicket First 的 P0 最小字段集

在字段列表中补充：

```text
- doneCriteria（建议字段）：节点完成判定标准，可先用 Markdown 列表表达
```

## 建议插入位置 3：3.5 DesignImplementationSync 后

```md
补充说明：DesignImplementationSync 不是独立执行层，而是围绕 TaskFlow / TaskTicket 发生的设计-实施同步流程；同步结果应回写为 TaskEvent、ReviewRecord、DecisionItem 或 HandoffPackage。
```

## 建议插入位置 4：Artifact / Evidence 相关段落

将 Evidence 示例统一为：

```text
Artifact / Change Reference:
- commit hash
- 文档路径
- 代码路径
- 原型 HTML
- QA 报告路径

Evidence:
- 测试输出
- 截图
- brokenImages=0 / pageErrors=0 / httpErrors=0
- 交付审查结论
```

说明：commit hash 更适合作为 artifact 引用或变更引用，不应单独作为验证证据。
```
